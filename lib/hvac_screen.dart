import 'package:flutter/material.dart';

class HVACScreen extends StatelessWidget {
  const HVACScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('HVAC'));
  }
}
